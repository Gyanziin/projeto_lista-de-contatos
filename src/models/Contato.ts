class Contato {
  nome: string
  tel: string
  email: string

  constructor(nome: string, tel: string, email: string) {
    this.nome = nome
    this.tel = tel
    this.email = email
  }
}

export default Contato
